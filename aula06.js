// Logic Operators

/*
    && / and
    || / or
    ! / (not)
*/

// && - all values ​​must be true.
console.log(10 == 10 && 5 == 5);

// || - only one of the values must be true.
console.log(10 == 10 || 10 < 5);

// inverts the value
console.log(10 > 1); // invert the value to false or true